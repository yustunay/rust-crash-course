Install tools
>>rust-analyzer
>>better toml

>> cargo init
>> cargo run
Runs compiled file in the debug folder
>> ./target/debug/rust_sandbox
Build
>> cargo build
Build for production
>> cargo build --release
